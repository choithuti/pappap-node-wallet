name: pappap_node_wallet
description: Official Mobile Wallet for Pappap AI Chain SNN - Made in Vietnam
version: 1.0.0+1
environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  flutter_tts: ^4.0.2
  speech_to_text: ^6.6.0
  url_launcher: ^6.2.4
  qr_flutter: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
